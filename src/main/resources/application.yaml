spring:
  application:
    name: notes
  datasource:
    url: jdbc:mysql://localhost:3306/securenotes
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
logging:
  level:
    org:
      springframework:
        security: DEBUG